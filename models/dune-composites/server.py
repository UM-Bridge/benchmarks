import umbridge
import json
import os
import csv

class DuneCompModel(umbridge.Model):
    def __init__(self):
        super().__init__("forward")

    def get_input_sizes(self, config):
        return [346000]

    def get_output_sizes(self, config):
        return [1]

    def __call__(self, parameters, config):

        # TODO: Write input coeff file (generated by MCMC)

        # add MOOSE Python libraries to PYTHONPATH, modify input files and run achlys
        os.system("")

        # Read results CSV file, write rows to output
        output = [1]

        print(f"output: {output}")

        return output

    def supports_evaluate(self):
        return True

model = AchlysModel()

umbridge.serve_models([model], 4242)
